<Window x:Class="FigmaWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Figma WPF App" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Colors.xaml"/>
                <ResourceDictionary Source="Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" 
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Figma WPF App" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center"/>
                
                <!-- Navigation Menu Button -->
                <Button Name="MenuButton" 
                        Content="☰" 
                        Style="{StaticResource ModernButtonStyle}"
                        Width="40" Height="40" 
                        HorizontalAlignment="Left" 
                        Margin="10,0,0,0"
                        Click="MenuButton_Click"/>
            </Grid>
        </Border>
        
        <!-- Content Area -->
        <Grid Grid.Row="1" Name="ContentArea">
            <!-- Default content frame for different views -->
            <Frame Name="MainFrame" 
                   NavigationUIVisibility="Hidden" 
                   Background="Transparent"/>
            
            <!-- Welcome Screen (Default) -->
            <StackPanel Name="WelcomePanel" 
                       Orientation="Vertical" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                
                <TextBlock Text="Welcome to Figma WPF App" 
                          FontSize="32" FontWeight="Light" 
                          Foreground="{StaticResource TextBrush}" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,30"/>
                
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    
                    <Button Content="Main Menu" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="120" Height="45" 
                            Margin="10" 
                            Click="ShowMainMenu_Click"/>
                    
                    <Button Content="Loading Demo" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Width="120" Height="45" 
                            Margin="10" 
                            Click="ShowLoading_Click"/>
                    
                    <Button Content="Options" 
                            Style="{StaticResource AccentButtonStyle}"
                            Width="120" Height="45" 
                            Margin="10" 
                            Click="ShowOptions_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryBrush}" 
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock Name="StatusText" 
                          Text="Ready" 
                          FontSize="12" 
                          Foreground="{StaticResource TextBrush}" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Left" 
                          Margin="10,0,0,0"/>
                
                <TextBlock Name="TimeText" 
                          FontSize="12" 
                          Foreground="{StaticResource TextBrush}" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Right" 
                          Margin="0,0,10,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
